# Topology

Edit `figure.tex`, then save for LaTeX Workshop preview or run the VSCode task
`Figure: Build PDF+SVG for current figure`.

Use filled regions sparingly; they survive PDF, SVG, and Obsidian export better than transparency-heavy drawings.
