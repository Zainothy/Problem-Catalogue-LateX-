# Free Body Diagram

Edit `figure.tex`, then save for LaTeX Workshop preview or run the VSCode task
`Figure: Build PDF+SVG for current figure`.

Use `fdeForce` for physical forces and keep labels short so the exported SVG stays readable in Obsidian.
