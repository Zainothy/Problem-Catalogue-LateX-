# Commutative Diagram

Edit `figure.tex`, then save for LaTeX Workshop preview or run the VSCode task
`Figure: Build PDF+SVG for current figure`.

This template uses `tikz-cd`, which works well for category theory, algebra, topology, and exact sequences.
