# Python Figures

Use this category when the figure is driven by computed data.

The standard pattern is:

```text
figure.py
  -> data.csv
  -> figure.tex
  -> figure.svg
```

The build script runs `figure.py` automatically before compiling `figure.tex`.
