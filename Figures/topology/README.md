# Topology

Use this category for topology, manifolds, homotopy, surfaces, and schematic spaces.

Good starting template:

```powershell
..\common\new-figure.ps1 -Category topology -Name my-space -Template topology
```
