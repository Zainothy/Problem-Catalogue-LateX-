{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: current figure generator",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "cwd": "${fileDirname}",
      "console": "integratedTerminal"
    }
  ]
}
